import React from 'react';
import ReactDOM from 'react-dom/client';
import PuzzlesApp from './puzzles/PuzzlesApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PuzzlesApp />
  </React.StrictMode>
);
